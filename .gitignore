# TA/faculty-only — NEVER commit to the student-facing repo (the builders contain the solution
# code inline, and these docs describe the answers / build internals). Kept local for instructors.
solutions/
_build/
NOTES.md
CLAUDE.md
utssrp_notebook_build_spec.md

# Real data + raw Zenodo downloads — too large for GitHub; kept local (see README "Get the data").
data/xp_apogee_real.npz
data/xp_apogee_cat.h5
data/*.zip
data/raw_zenodo/
*.h5

# Notebook + Python cruft
.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store

# Student run artifacts (regenerated by the notebooks; not source)
outputs/*.npz
outputs/*.csv
figures/*.png
figures/*.pdf
models/*.joblib
!outputs/.gitkeep
!figures/.gitkeep
!models/.gitkeep
